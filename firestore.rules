rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Permissive rules for Saudi Core Controls and Madar operations
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
